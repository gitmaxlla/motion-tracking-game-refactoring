- pose_predicates - логичнее множественное число, либо может вообще pos, как так не все файлы - предикаты.
- не хватает уровней абстракции в файлах из pose_predicate:
* helper-файл, который бы извлекал нужные позиции (условно: Landmarks.getShoulder('R'), Landmarks.getPinkyTip('L'))
  и возвращал бы соотвествующий словарь, чтобы убрать дублирование и убрать зависимости в коде?
* (все методы далее можно перенести туда же)
* areCoordsClose() -> closeToEachOther()?
* a.y < b.y -> lowerThan(a, b)
* a.x > b.x -> outsideOf(a, b)
* ... а так же все остальные повторяющиеся шаблоны сравнений 
* вместо всей идеи с разделением поз на функции-предикаты стоит, возможно, вынести всё в интерфейс с методом check()
  для проверки позы (turnLeftPose.check(...))
* *Removed* позы не очень понятны и не вписываются в общую идею (поза остановки, поза левого поворота, поза снятых ремней(?))
  -> следует переименовать